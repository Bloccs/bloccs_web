// PLACEHOLDER bundle. The real app.js is produced by `mix assets.build`
// (esbuild over assets/js/app.js), which bundles Phoenix + LiveView and the
// GraphLayout hook. It is committed here so the package ships without a consumer
// Node build; CI's `mix assets.verify` rebuilds it and fails on drift.
//
// This stub is intentionally inert until the asset toolchain runs once (it needs
// `mix deps.get` + `assets/node_modules`). Replace by running:
//   mix assets.build
console.warn("bloccs_web: run `mix assets.build` to generate the real app.js bundle")
